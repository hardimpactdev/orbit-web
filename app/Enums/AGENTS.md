# Enums Directory

PHP enums for type-safe constants.

## Requirements

- Use backed enums (`string` or `int`) for database storage
- Add descriptive methods like `label()` for UI display
- Use for status fields, types, categories
